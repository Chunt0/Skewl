{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "source": "src/index.ts",
  "scripts": {
    "build": "bunx tsc -p tsconfig.json",
    "start": "bun run dist/index.js",
    "dev": "bun --watch src -- bunx ts-node-esm src/index.ts",
    "clean": "rm -rf dist && mkdir -p dist"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "@types/bun": "latest",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "ts-node-esm": "^1.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}

